[pytest]
pythonpath = .
testpaths = tests
addopts = 
    --verbose
    --cov=app
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=50
markers =
    slow: marks tests as slow
    integration: marks tests as integration tests
